# Add "Keeps It Current" UI

This connects to the theme phrase "keeps it current."
Use only if you have extra time:
Add an optional "Update Skill" section to show how SkillBrain keeps company knowledge current.

The section should include:
- textarea labeled "New company update"
- button "Update Skill"
- output area titled "Change Log"

Use mock logic if backend update logic is not ready.

Example update:
Refunds above $200 require manager approval instead of $100.

Expected change log:
- Modified rule: manager approval threshold changed from $100 to $200.
- Skill file updated to reflect the new escalation rule.

This should be visually secondary and should not distract from the main demo flow.
